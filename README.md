# The TLS 1.3 in Go

This library is a stanalone dependency-free TLS 1.3 implementation in
Go. It implements both TLS client and server functionalities with the
minimal set of "MUST" requirements from the [RFC
8446](https://www.rfc-editor.org/rfc/rfc8446.html).
